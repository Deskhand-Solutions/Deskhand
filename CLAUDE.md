# Deskhand — Claude Code Instructions

This file is the Claude Code entry point. Content mirrors `AGENTS.md`.

**Rules:** Claude Code loads modular rules from `.claude/rules/` (synced from `agent-rules/`).
After editing rules, run `.\scripts\sync-agent-rules.ps1`.

@import AGENTS.md
